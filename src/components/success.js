import React from 'react';

export default () => {
  // if logged in greet, the user
  if (null) {
    return (
      <div>Hi, username! You have logged in successfully.</div>
    );
  } else {
    return (
      <div>You are not logged in.</div>
    )
  }
}
